<h1>Admins::Items#show</h1>
<p>Find me in app/views/admins/items/show.html.erb</p>

<p><%= @artist.artist_name %>-<%= @item.album_name %>-詳細</p>

<p>含まれているdisc一覧</p>
<table>
  <tr>
    <th>type</th>
    <th>編集</th>
  </tr>
  <% @discs.each do |disc| %>
  <tr>
    <td><%= disc.disc_type %></td>
    <td><%= link_to "このdiscを編集する", admins_artist_item_disc_path(@artist, @item, disc) %></td>
  </tr>
  <% end %>
</table>